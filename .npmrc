# Always install devDependencies during clean production builds.
# Without this, hosts that set NODE_ENV=production skip devDependencies
# and the build tools (vite, esbuild, typescript) are missing.
production=false
